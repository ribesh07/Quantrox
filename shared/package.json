{
  "name": "@quantrox/shared",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "npm run prisma:generate && tsc",
    "prisma:generate": "prisma generate --schema=./prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.19.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "prisma": "^6.19.1",
    "typescript": "^5.6.3"
  }
}
